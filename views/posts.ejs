<%- include('header') %>
<div class="nav">
  <div class="ui grid">
    <div class="four wide column"></div>

    <div class="eight wide column">
      <span>
        <form action="/search" method="GET">
          <input type="text" name="postId" placeholder="在此输入电池信息以搜索" class="search" />
        </form>
      </span>
    </div>
  </div>
</div>
<% posts.forEach(function (post) { %>
  <%- include('components/post-content', { post: post }) %>
<% }) %>

<br />
<div>
    <% if (!isFirstPage) { %>
  <span class="prepage"><a title="上一页" href="?p=<%= page-1 %>">上一页</a></span>
    <% } %>

    <% if (!isLastPage) { %>
  <span class="nextpage"><a title="下一页" href="?p=<%= page+1 %>">下一页</a></span>
    <% } %>
</div>
<%- include('footer') %>